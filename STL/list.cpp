//we know that list is implemented from doubly link list so we cant directly acces that ele
//But in dequq we use static array so we can directly acces that ele and use function at
//
#include<iostream>
using namespace std;
#include<list>
int main()
{
list<int> l;
l.push_front(1);
l.push_back(2);
 l.erase(l.begin(),l.end());

cout<<"Sjcbhu";
}